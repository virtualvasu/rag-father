in the UI:
0. no UI confirmation after testset for for eval. generated
1. ensure for each checkbox/feature, user sees info about what that feature does
2. after eval, and every where in the website show the scores in percentages instead of decimals


