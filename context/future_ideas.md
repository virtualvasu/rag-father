1. improve system prompt while giving final prompt to llm for answer gen.
1.1. instyead   of just focusing on legal rag now, ragfather is a general purpose rag builder, can no longer use the old system prompts , can we add a functionality for the user too add the system prompts themselves, or maybe user can enter the context and we can generate a good system prompt for fufture use in the pipeline for that particular rag by llm (ollmaa or any llm which user chooses)

2. replace clause with rag  father in forntend everywhere and remove the legal rag terms 

3. logs from pipeline and eval in frontend