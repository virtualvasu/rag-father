1. if we could add a simple indexed db for saving all the evaluation results so the user can compare among various trainings and select the best set of features for him, show him for each training what files he used, and all the params of pipeline and also params of eval pipeline 

2. add this as an option in the eval page, to see , but mention that to get the same rag again, you need to train using same params if the desired set of params is not from the last training, in that case, pipeline has already  been trained on those features

3. convert back to docker container for the full ragfather application.